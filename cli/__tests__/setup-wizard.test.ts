// Phase 03 Plan 01 (SETUP-02): Wave-0 test scaffold — downstream plans fill in bodies.
import { describe, it } from 'vitest'

describe('dtc setup sections', () => {
  it.todo('dtc setup firebase creates project')
  it.todo('dtc setup firebase registers iOS app')
  it.todo('dtc setup firebase downloads GoogleService-Info.plist')
  it.todo('dtc setup apple stores ASC key path and IDs')
  it.todo('dtc setup oauth stores Apple Sign In service ID')
  it.todo('dtc setup --full runs all sections in order')
})
