// Generated by appifex-dtc — mock provider
// MOCK ONLY — switch to live backend by replacing Mock* return values in this file
import Foundation

enum ServiceConfiguration {
    static func makeAuthManager() -> some AuthManaging {
        return MockAuthManager.shared
    }

<% for (const entity of it.schema.entities) { %>
    static func make<%= entity.name %>Repository() -> some <%= entity.name %>Repository {
        return Mock<%= entity.name %>Repository()
    }

<% } %>
}
