# Technology Stack

**Analysis Date:** 2026-04-14

## Languages

**Primary:**
- TypeScript ^5.7.0 - All source code across `packages/*`, `cli/`, and `sidecar/`

**Secondary:**
- JavaScript (ES2022) - Compiled `dist/` output; shebang entry at `bin/dtc`
- Target platforms generated by the toolkit (not authored here):
  - Swift / SwiftUI - via `@dtc/build` (`packages/build/dist/swift-archive.d.ts`, `swift-precheck.d.ts`)
  - Kotlin / Jetpack Compose - via `@dtc/build` (`packages/build/dist/kotlin.d.ts`, `kotlin-bundle.d.ts`)
  - React - referenced as a supported platform in `bin/dtc`

## Runtime

**Environment:**
- Node.js >= 22.0.0 (enforced via `engines` in `package.json`)
- Module system: ESM (`"type": "module"` across all workspace packages)
- TypeScript module resolution: `Node16` (see `tsconfig.json`)

**Package Manager:**
- pnpm >= 9.0.0 (enforced via `engines`)
- Workspace config: `pnpm-workspace.yaml` (globs `packages/*` and `cli`)
- Lockfile: `pnpm-lock.yaml` present at repo root
- `pnpm.onlyBuiltDependencies`: `["better-sqlite3"]`

## Frameworks

**Core:**
- Fastify (sidecar HTTP server) - `sidecar/dist/server.js`, imported as `import Fastify from 'fastify'`
- Model Context Protocol SDK `@modelcontextprotocol/sdk` ^1.29.0 - `packages/mcp-server/src/server.ts`, `packages/mcp-server/src/index.ts` (StdioServerTransport)
- Ink ^5.2.0 (React-based terminal UI) - CLI rendering in `cli/src/views/`
- React ^18.3.0 - Paired with Ink for CLI views

**Testing:**
- Vitest ^4.0.0 - Runner for all tests (`vitest.config.ts`); includes `packages/**/__tests__/**/*.test.ts` and `cli/__tests__/**/*.test.ts`

**Build/Dev:**
- TypeScript compiler (`tsc`) - Each package builds via `tsc` to `dist/`
- Build orchestration: root `package.json` `build` script uses `pnpm --filter` to order `@dtc/core` then `@dtc/runner` then everything else then `appifex-dtc`
- Custom setup script: `scripts/build-and-link.mjs` (referenced by `pnpm setup`)
- Lint: `tsc --noEmit` (no ESLint/Prettier configured)

## Key Dependencies

**LLM / AI SDKs:**
- `@anthropic-ai/sdk` ^0.52.0 - Used in `packages/codegen/src/default-generate.ts`, `packages/codegen/src/layered-generate.ts`, `packages/fix/src/default-fix.ts`, `packages/baas/package.json`, `cli/src/pipeline.ts`
- `@github/copilot-sdk` ^0.2.0 - Used in `cli/src/pipeline.ts` and `cli/src/providers/copilot.ts`
- OpenAI - Called via raw HTTPS (`https://api.openai.com/v1`) in `cli/src/pipeline.ts:701`; no SDK package
- Google Gemini - Called via raw HTTPS (`https://generativelanguage.googleapis.com/v1beta/...`) in `cli/src/pipeline.ts:672`
- Claude Code CLI - Spawned as subprocess (`packages/codegen/src/claude-cli-generate.ts`)

**Design tooling SDKs:**
- `@google/stitch-sdk` ^0.1.0 - `packages/design/src/stitch-adapter.ts`
- Pencil MCP client - Custom implementation `packages/design/src/pencil-mcp-client.ts` using MCP SDK
- Figma REST client - Custom `packages/design/src/figma-rest-client.ts` + `figma-make-adapter.ts`

**Google Play Publishing:**
- `@googleapis/androidpublisher` ^14.1.0 - `packages/provision/src/play-console-client.ts`

**Template engine:**
- `eta` ^3.5.0 - Used by `@dtc/baas` for rendering BaaS templates (`packages/baas/src/render-templates.ts`, templates in `packages/baas/src/templates/`)

**Storage / Persistence:**
- `better-sqlite3` ^11.0.0 - `packages/core/src/checkpoint.ts` (checkpoint/resume DB)
- Types: `@types/better-sqlite3` ^7.6.0

**Schema validation:**
- `zod` ^3.24.0 - `packages/mcp-server/src/server-tools-dev.ts`, `server-tools-pipeline.ts` (MCP tool input schemas)

**CLI UX:**
- `@clack/prompts` ^0.10.0 - Interactive setup wizard (`cli/src/setup-wizard.ts`)
- `chalk` ^5.4.0 - Terminal colors (`cli/src/entry.ts` and throughout)
- `ink-spinner` ^5.0.0 - Progress spinners

**Types:**
- `@types/node` ^22.0.0
- `@types/react` ^18.3.0 (cli dev dep)

## Configuration

**TypeScript:**
- Root `tsconfig.json`: `target: ES2022`, `module: Node16`, `strict: true`, `declaration: true`, `sourceMap: true`, `verbatimModuleSyntax: true`, `isolatedModules: true`, `rootDir: src`, `outDir: dist`
- Per-package `tsconfig.json` extends root (e.g., `cli/tsconfig.json`)

**Vitest:**
- `vitest.config.ts` defines aliases for all `@dtc/*` workspace packages pointing at `src/index.ts` (not `dist/`), enabling source-level testing without building
- `testTimeout: 10_000`

**Runtime config file:**
- `config.json` loaded/saved via `packages/core/src/config.ts`
- Default location: `~/.dtc/config.json` (see `cli/src/setup-wizard.ts:11`)
- Override via `configDir` argument
- Shape defined in `packages/core/src/types-config.ts` (`DtcConfig`)

**Environment variables consumed:**
- `PENCIL_CLI_KEY` - `packages/design/src/adapter-factory.ts:132`
- `STITCH_API_KEY` - `packages/design/src/adapter-factory.ts:136`
- `FIGMA_TOKEN` - `packages/design/src/adapter-factory.ts:140`
- `ANDROID_HOME` / `ANDROID_SDK_ROOT` - `packages/core/src/prerequisites.ts:144`, `packages/runner/src/local-runner.ts:29`
- `DTC_GITHUB_CLIENT_ID` - `cli/src/setup-wizard.ts:12`
- `HOME`, `PATH` - `packages/validate/src/maestro.ts`

## Platform Requirements

**Development:**
- Node >= 22
- pnpm >= 9
- macOS required for iOS/Swift builds (xcodebuild, xcrun referenced in `packages/validate/src/` and `packages/build/`)
- Android SDK for Android builds
- Maestro for UI testing (`packages/validate/src/maestro.ts`) - PATH augmented with `~/.maestro/bin`
- Semgrep for security scanning (`packages/validate/src/semgrep.ts`)

**Production / Execution runtimes (Runner abstraction):**
- Local runner - `packages/runner/src/local-runner.ts` (spawn/execSync)
- E2B remote sandbox - `packages/runner/src/e2b-runner.ts` (`https://api.e2b.dev`), requires `sandboxId` + `e2bApiKey`
- Remote runner - `packages/runner/src/remote-runner.ts` (custom HTTP runner via `runnerUrl` + `runnerToken`)

## Distribution

**Entry points:**
- CLI binary: `cli/package.json` `bin.dtc` -> `./dist/entry.js` (also compiled at `bin/dtc` directory as legacy artifact)
- MCP server binary: `packages/mcp-server/package.json` `bin.dtc-mcp-server` -> `./dist/index.js`
- Library exports: each package exposes `./dist/index.js` via `exports.import`
- Sidecar HTTP service: `sidecar/dist/server.js` (Fastify)

---

*Stack analysis: 2026-04-14*
