---
phase: 08-formal-verification-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-foundation-hardening/02-VERIFICATION.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "02-VERIFICATION.md exists and its frontmatter status is 'passed'"
    - "Every FOUND-01..04 row in the requirements table cites a test file name, assertion count, and commit hash"
    - "The behavioral spot-checks table includes a live pnpm test output row (files / passed / skipped)"
    - "The verdict section says GOAL_ACHIEVED and status is passed"
  artifacts:
    - path: ".planning/phases/02-foundation-hardening/02-VERIFICATION.md"
      provides: "Formal verification report for Phase 2 Foundation Hardening"
      contains: "FOUND-01"
  key_links:
    - from: "02-VERIFICATION.md requirements table"
      to: "02-VALIDATION.md evidence"
      via: "test file names and assertion counts"
      pattern: "FOUND-0[1-4]"
    - from: "02-VERIFICATION.md behavioral spot-checks"
      to: "live pnpm test output"
      via: "D-01 requirement — run pnpm test at execution start"
      pattern: "pnpm test"
---

<objective>
Write the Phase 2 Foundation Hardening formal verification report.

Purpose: Formally close FOUND-01..04 by producing a VERIFICATION.md that proves all Phase 2 success criteria are satisfied in the repository. This is the documentary artifact that moves these requirements from "Pending" to "Verified" in the traceability table.

Output: `.planning/phases/02-foundation-hardening/02-VERIFICATION.md` — a complete verification report following the Phase 1 VERIFICATION.md format exactly.
</objective>

<execution_context>
@/Users/rayliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rayliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-open-source-release-readiness/VERIFICATION.md
@.planning/phases/02-foundation-hardening/02-VALIDATION.md
@.planning/phases/02-foundation-hardening/02-UAT.md
@.planning/phases/08-formal-verification-foundation-setup/08-CONTEXT.md
@.planning/phases/08-formal-verification-foundation-setup/08-PATTERNS.md

<interfaces>
<!-- Format contract extracted from Phase 1 VERIFICATION.md (the canonical template) -->

YAML frontmatter shape (Phase 2 — no human_verification block needed):
```yaml
---
phase: 02-foundation-hardening
verified: <ISO-8601 timestamp>
status: passed
score: 4/4 requirements verified in code
overrides_applied: 0
---
```

Requirements table columns: `# | REQ-ID | Description | Status | Evidence`
Each Evidence cell must cite: test file name(s) + assertion count + commit hash + key structural check.

Required Artifacts table columns: `Artifact | Status | Details`

Behavioral Spot-Checks table columns: `Behavior | Command | Result | Status`

Anti-Patterns Found table columns: `File | Pattern | Severity | Impact`

Verdict section must use: `**GOAL_ACHIEVED**` and `Status is \`passed\`.`

Phase 2 evidence map from 08-PATTERNS.md:
- FOUND-01: test files `cli/__tests__/bin-dtc-launcher.test.ts`, `__tests__/prepare-hook.test.ts`; commits from 02-04-SUMMARY + 02-05-SUMMARY
- FOUND-02: test files `packages/analysis/__tests__/token-cap.test.ts`, `packages/core/__tests__/token-budget.test.ts`, `packages/fix/__tests__/fix-loop-budget-guard.test.ts`
- FOUND-03: test files `cli/__tests__/pipeline-epipe.test.ts`, `packages/codegen/__tests__/claude-cli-epipe.test.ts`, `packages/fix/__tests__/claude-cli-fix-epipe.test.ts`, `packages/agent/__tests__/base-adapter-epipe.test.ts`
- FOUND-04: test files `cli/__tests__/preflight.test.ts`, `packages/mcp-server/__tests__/cli-error-translation.test.ts`, `cli/__tests__/entry-error-handling.test.ts`; commits `bec8d15`, `565021a`, `ae05ed6`, `54387dd` (from 08-PATTERNS.md)
- Total Phase 2: 12 test files, 48 assertions, 1.01s (02-VALIDATION.md audit)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather evidence — read Phase 2 source files directly</name>
  <files>
    (read-only task — no files written; evidence assembled in memory for Task 2)
  </files>
  <read_first>
    - .planning/phases/02-foundation-hardening/02-VALIDATION.md (per-task verification map + audit counts)
    - .planning/phases/02-foundation-hardening/02-UAT.md (UAT results and status)
    - .planning/phases/02-foundation-hardening/02-01-SUMMARY.md (FOUND-04 commits)
    - .planning/phases/02-foundation-hardening/02-02-SUMMARY.md (FOUND-02 commits)
    - .planning/phases/02-foundation-hardening/02-03-SUMMARY.md (FOUND-03 commits)
    - .planning/phases/02-foundation-hardening/02-04-SUMMARY.md (FOUND-01 bin/dtc commits)
    - .planning/phases/02-foundation-hardening/02-05-SUMMARY.md (FOUND-01 prepare-hook commits)
    - cli/__tests__/bin-dtc-launcher.test.ts (FOUND-01 — read actual test assertions)
    - __tests__/prepare-hook.test.ts (FOUND-01 — read actual test assertions)
    - packages/analysis/__tests__/token-cap.test.ts (FOUND-02 — read assertion count)
    - packages/core/__tests__/token-budget.test.ts (FOUND-02 — read assertion count)
    - packages/fix/__tests__/fix-loop-budget-guard.test.ts (FOUND-02 — read assertion count)
    - cli/__tests__/pipeline-epipe.test.ts (FOUND-03)
    - packages/codegen/__tests__/claude-cli-epipe.test.ts (FOUND-03)
    - packages/fix/__tests__/claude-cli-fix-epipe.test.ts (FOUND-03)
    - packages/agent/__tests__/base-adapter-epipe.test.ts (FOUND-03)
    - cli/__tests__/preflight.test.ts (FOUND-04)
    - packages/mcp-server/__tests__/cli-error-translation.test.ts (FOUND-04)
    - cli/__tests__/entry-error-handling.test.ts (FOUND-04)
    - packages/core/src/errors.ts (Required artifact — confirm CliError hierarchy)
    - cli/src/pipeline.ts (confirm EpipeError at 4 sites — grep for EpipeError)
    - packages/codegen/src/claude-cli-generate.ts (EpipeError site 2)
    - packages/fix/src/default-fix.ts (EpipeError site 3)
    - packages/agent/src/adapters/base.ts (EpipeError site 4)
    - packages/mcp-server/src/server.ts (confirm wrapToolHandler / process.exit guard)
    - bin/dtc (confirm portable shim — not absolute symlink)
  </read_first>
  <action>
    Per D-03: do not summarize from SUMMARY.md alone — read source files directly to extract:

    1. Run `pnpm test` at execution start (per D-01). Record exact output line: "Test Files N passed / Tests M passed / K skipped / duration". This goes in the behavioral spot-checks table.

    2. For each FOUND-* requirement, extract from source:
       - Exact test file names (confirm they exist via Read tool)
       - Assertion count per file (count `it(`, `test(`, `expect(` calls or read the test runner output)
       - Commit hashes from SUMMARY.md files (read the SUMMARY files for commits)
       - Key structural checks: e.g., for FOUND-04, confirm `wrapToolHandler` exists in mcp-server/src/server.ts; for FOUND-01, confirm `bin/dtc` is a JS shim (not a symlink); for FOUND-02, confirm `CHARS_PER_TOKEN = 3` in the relevant source; for FOUND-03, confirm `EpipeError` appears in exactly 4 source files.

    3. Run targeted Phase 2 vitest command to get isolated confirmation:
       ```
       pnpm vitest run cli/__tests__/bin-dtc-launcher.test.ts packages/analysis/__tests__/token-cap.test.ts packages/core/__tests__/token-budget.test.ts packages/fix/__tests__/fix-loop-budget-guard.test.ts cli/__tests__/pipeline-epipe.test.ts packages/codegen/__tests__/claude-cli-epipe.test.ts packages/fix/__tests__/claude-cli-fix-epipe.test.ts packages/agent/__tests__/base-adapter-epipe.test.ts cli/__tests__/preflight.test.ts packages/mcp-server/__tests__/cli-error-translation.test.ts cli/__tests__/entry-error-handling.test.ts __tests__/prepare-hook.test.ts
       ```
       Expected: 12 files passed / 48 tests passed.

    4. Confirm UAT status from 02-UAT.md (should be `status: complete`, all checks passed).

    Assemble all evidence strings — they will be written verbatim into the VERIFICATION.md in Task 2.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -5</automated>
  </verify>
  <done>Live pnpm test output captured; all 12 Phase 2 test files confirmed green; commit hashes extracted from SUMMARY.md files; key structural checks confirmed (EpipeError in 4 files, wrapToolHandler present, bin/dtc is shim, CHARS_PER_TOKEN=3).</done>
</task>

<task type="auto">
  <name>Task 2: Write 02-VERIFICATION.md</name>
  <files>.planning/phases/02-foundation-hardening/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-open-source-release-readiness/VERIFICATION.md (canonical format — replicate structure exactly)
    - .planning/phases/08-formal-verification-foundation-setup/08-PATTERNS.md (frontmatter pattern, table patterns, verdict pattern)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/02-foundation-hardening/02-VERIFICATION.md`.

    Follow the Phase 1 VERIFICATION.md structure exactly. Required sections in order:
    1. YAML frontmatter with: `phase: 02-foundation-hardening`, `verified: <current ISO-8601 timestamp>`, `status: passed`, `score: 4/4 requirements verified in code`, `overrides_applied: 0`
    2. Document header: `# Phase 2: Foundation Hardening Verification Report` with Phase Goal, Verified timestamp, Status
    3. `## Goal Achievement` section containing:
       a. `### Requirements Coverage` table — 4 rows (FOUND-01 through FOUND-04), columns: `# | REQ-ID | Description | Status | Evidence`. Evidence cells MUST include test file names, assertion counts, and commit hashes gathered in Task 1. Never use vague evidence like "tests pass" — cite specific files.
       b. Score summary line after the table
       c. `### Required Artifacts` table — list the key files created by Phase 2 (errors.ts, bin/dtc, token-budget extensions, EpipeError additions, mcp-server wrapToolHandler, etc.) with VERIFIED status and specific details
       d. `### Behavioral Spot-Checks` table — include: `pnpm test` (live output from Task 1), targeted Phase 2 vitest run, EpipeError grep (4 files), bin/dtc --help exit 0, CHARS_PER_TOKEN grep result, wrapToolHandler grep result
       e. `### Anti-Patterns Found` table — include the empty/info-only rows (e.g., any pre-existing it.skip annotations if found)
    4. `### Gaps Summary` — state "Zero code-level gaps remain. All 4 requirements are satisfied."
    5. `### Verdict` — `**GOAL_ACHIEVED**` — all 4 requirements satisfied in code. Status is `passed`.
    6. Footer: `_Verified: <timestamp>_` / `_Verifier: Claude (gsd-verifier)_`

    Concrete values for evidence cells (supplement with Task 1 findings):
    - FOUND-01: `cli/__tests__/bin-dtc-launcher.test.ts` (spawns bin/dtc --help, asserts exit 0) + `__tests__/prepare-hook.test.ts` (prepare script guard for core.hooksPath + missing .git). Commits from 02-04-SUMMARY and 02-05-SUMMARY.
    - FOUND-02: `packages/analysis/__tests__/token-cap.test.ts` (Swift density CHARS_PER_TOKEN=3) + `packages/core/__tests__/token-budget.test.ts` (canEnterFixLoop threshold cases) + `packages/fix/__tests__/fix-loop-budget-guard.test.ts` (BudgetExhaustedError short-circuit). Commits from 02-02-SUMMARY.
    - FOUND-03: `cli/__tests__/pipeline-epipe.test.ts` + `packages/codegen/__tests__/claude-cli-epipe.test.ts` + `packages/fix/__tests__/claude-cli-fix-epipe.test.ts` + `packages/agent/__tests__/base-adapter-epipe.test.ts` — 4 files, mocked spawn stdin EPIPE → EpipeError. Commits from 02-03-SUMMARY.
    - FOUND-04: `cli/__tests__/preflight.test.ts` (PreflightError no exit) + `packages/mcp-server/__tests__/cli-error-translation.test.ts` ({isError:true} envelope, host alive) + `cli/__tests__/entry-error-handling.test.ts` (CliError → chalk + exit code). Commits: bec8d15, 565021a, ae05ed6, 54387dd (verify from SUMMARY files).

    Phase 2 aggregate: 12 test files, 48 assertions, all green.
  </action>
  <verify>
    <automated>grep -c "FOUND-0" .planning/phases/02-foundation-hardening/02-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-foundation-hardening/02-VERIFICATION.md` exists
    - `grep "status: passed" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns a match in frontmatter
    - `grep -c "FOUND-0" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns at least 4
    - `grep "GOAL_ACHIEVED" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns a match
    - `grep "score: 4/4" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns a match
    - `grep "pnpm test" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns at least 1 match (behavioral spot-checks row)
    - `grep "bin-dtc-launcher.test.ts" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns a match (FOUND-01 evidence)
    - `grep "pipeline-epipe.test.ts" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns a match (FOUND-03 evidence)
    - `grep "cli-error-translation.test.ts" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` returns a match (FOUND-04 evidence)
    - File has `phase: 02-foundation-hardening` in YAML frontmatter (line 2)
  </acceptance_criteria>
  <done>02-VERIFICATION.md written, all 4 FOUND-* requirements have evidence rows with test file names + assertion counts + commit hashes, behavioral spot-checks table includes live pnpm test output, verdict is GOAL_ACHIEVED with status passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VALIDATION.md → VERIFICATION.md | Evidence read from existing planning artifacts and cited in new verification doc — risk of misquoting or cherry-picking |
| Source files → evidence claims | Executor reads source files directly; risk of citing incorrect line numbers, wrong commit hashes, or stale assertion counts |
| VERIFICATION.md → REQUIREMENTS.md | VERIFICATION.md verdict drives REQUIREMENTS.md status update in Plan 03 — a false PASS here propagates upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | Evidence cells in requirements table | mitigate | Task 1 reads source files directly (not SUMMARY.md alone) per D-03; Task 2 action specifies exact test file names and commit sources to cite |
| T-08-01-02 | Information Disclosure | False negative — requirement marked PASS when test actually fails | mitigate | D-01 mandates running `pnpm test` live at execution start; spot-checks table must cite the live output, not historical VALIDATION.md counts alone |
| T-08-01-03 | Repudiation | Executor skips reading source files and writes from memory/summaries | mitigate | `<read_first>` lists all 20 files the executor MUST read before writing; acceptance_criteria require specific test file names appear in the output |
| T-08-01-04 | Elevation of Privilege | Wrong requirement ID mapped (e.g., FOUND-03 evidence cited for FOUND-02 row) | mitigate | Evidence map in `<interfaces>` block maps each FOUND-* to its exact test files; acceptance_criteria grep for specific test file names per requirement |
</threat_model>

<verification>
After Task 2 completes:
- `cat .planning/phases/02-foundation-hardening/02-VERIFICATION.md` — review format matches Phase 1 template
- `grep "status: passed" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` — frontmatter status correct
- `grep -c "FOUND-0" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` — returns ≥ 4
- `grep "GOAL_ACHIEVED" .planning/phases/02-foundation-hardening/02-VERIFICATION.md` — verdict present
</verification>

<success_criteria>
1. `.planning/phases/02-foundation-hardening/02-VERIFICATION.md` exists with `status: passed` frontmatter
2. Requirements table has 4 rows (FOUND-01..04), each with: test file name(s), assertion count, commit hash, structural check result
3. Behavioral spot-checks table includes live `pnpm test` output row with actual file/test counts
4. Verdict is `GOAL_ACHIEVED`, status is `passed`, score is `4/4 requirements verified in code`
5. No evidence row says only "tests pass" without citing a specific file
</success_criteria>

<output>
After completion, create `.planning/phases/08-formal-verification-foundation-setup/08-01-SUMMARY.md`
</output>
