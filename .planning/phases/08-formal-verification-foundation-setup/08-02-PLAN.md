---
phase: 08-formal-verification-foundation-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "03-VERIFICATION.md exists and its frontmatter status is 'human_needed'"
    - "Score frontmatter is '4/4 requirements verified in code + 0/1 human checkpoint pending'"
    - "SETUP-04 row uses 'PASS (code-level)' status with inline note about deferred live OAuth"
    - "human_verification array is present with the Firebase live-project-creation test"
    - "The behavioral spot-checks table includes a live pnpm test output row"
    - "Verdict section says GOAL_ACHIEVED in code, GAPS_REMAIN for live Firebase confirmation"
  artifacts:
    - path: ".planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md"
      provides: "Formal verification report for Phase 3 Setup & Diagnostics"
      contains: "SETUP-01"
  key_links:
    - from: "03-VERIFICATION.md SETUP-04 row"
      to: "human_verification section"
      via: "D-02 — human_needed pattern from Phase 1"
      pattern: "human_needed"
    - from: "03-VERIFICATION.md behavioral spot-checks"
      to: "live pnpm test output"
      via: "D-01 requirement"
      pattern: "pnpm test"
---

<objective>
Write the Phase 3 Setup & Diagnostics formal verification report.

Purpose: Formally close SETUP-01..04 at the code level by producing a VERIFICATION.md that proves all Phase 3 success criteria are satisfied in the repository, while correctly deferring the live Firebase project creation step (SETUP-04 task 03-04-T2) to a human_verification entry. This document moves SETUP-01..04 from "Pending" to "Verified (code-level; SETUP-04 human step pending)" in the traceability table.

Output: `.planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` — a complete verification report following the Phase 1 VERIFICATION.md format with the `human_needed` status variant.
</objective>

<execution_context>
@/Users/rayliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rayliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-open-source-release-readiness/VERIFICATION.md
@.planning/phases/03-setup-and-diagnostics/03-VALIDATION.md
@.planning/phases/03-setup-and-diagnostics/03-UAT.md
@.planning/phases/08-formal-verification-foundation-setup/08-CONTEXT.md
@.planning/phases/08-formal-verification-foundation-setup/08-PATTERNS.md

<interfaces>
<!-- Format contract extracted from Phase 1 VERIFICATION.md (canonical template) -->

YAML frontmatter shape (Phase 3 — includes human_verification block):
```yaml
---
phase: 03-setup-and-diagnostics
verified: <ISO-8601 timestamp>
status: human_needed
score: 4/4 requirements verified in code + 0/1 human checkpoint pending
overrides_applied: 0
human_verification:
  - test: "Run `dtc setup firebase` 'create new project' branch with a real Google account + real GCP billing account"
    expected: "New Firebase project created, iOS app registered, GoogleService-Info.plist downloaded to project tree, config saved. Subprocess errors surface verbatim on conflict."
    why_human: "Requires live firebase CLI login and real GCP billing account. Task 03-04-T2 was explicitly designated as a human-verify checkpoint in the plan — cannot be automated in CI."
---
```

Requirements table: SETUP-04 row uses `PASS (code-level)` in Status column with inline note.

Phase 3 evidence map from 08-PATTERNS.md:
- SETUP-01: `cli/__tests__/setup-wizard.test.ts`, `cli/__tests__/setup-sections.test.ts`; source: `cli/src/setup/index.ts`, `cli/src/setup/project.ts` + 7 section files
- SETUP-02: `cli/__tests__/preflight-credential-gate.test.ts` (5 integration tests), `packages/core/__tests__/credential-registry.test.ts`, `packages/core/__tests__/asc-jwt.test.ts`, `packages/core/__tests__/config-permissions.test.ts`; source: `packages/core/src/credential-registry.ts`, `packages/core/src/asc-jwt.ts`, `cli/src/preflight.ts`
- SETUP-03: `cli/__tests__/doctor.test.ts`; source: `cli/src/doctor.ts`, `packages/core/src/prerequisites.ts`
- SETUP-04: `cli/__tests__/setup-firebase.test.ts`, `packages/baas/__tests__/firebase-provision.test.ts` (stub); source: `cli/src/setup/firebase.ts`
- Phase 3 total: 55 tests passing, 0 todos (03-VALIDATION.md audit)
- UAT (03-UAT.md): 7 passed / 1 skipped (test 5: Firebase live — manual-only). UAT inline fix: entry.ts missing await before runPreflight — fixed commit 976cfe7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather evidence — read Phase 3 source files directly</name>
  <files>
    (read-only task — no files written; evidence assembled in memory for Task 2)
  </files>
  <read_first>
    - .planning/phases/03-setup-and-diagnostics/03-VALIDATION.md (per-task verification map + audit counts)
    - .planning/phases/03-setup-and-diagnostics/03-UAT.md (UAT results, skipped test, entry.ts await fix commit)
    - .planning/phases/03-setup-and-diagnostics/03-01-SUMMARY.md (SETUP-02 scaffold commits)
    - .planning/phases/03-setup-and-diagnostics/03-02-SUMMARY.md (SETUP-02 probe commits)
    - .planning/phases/03-setup-and-diagnostics/03-03-SUMMARY.md (SETUP-01 wizard sectioning commits)
    - .planning/phases/03-setup-and-diagnostics/03-04-SUMMARY.md (SETUP-04 firebase section commits)
    - .planning/phases/03-setup-and-diagnostics/03-05-SUMMARY.md (SETUP-03 doctor commits)
    - cli/__tests__/setup-wizard.test.ts (SETUP-01 — read actual assertions)
    - cli/__tests__/setup-sections.test.ts (SETUP-01 — read actual assertions, if exists; else note absence)
    - cli/__tests__/preflight-credential-gate.test.ts (SETUP-02 — 5 integration tests)
    - packages/core/__tests__/credential-registry.test.ts (SETUP-02)
    - packages/core/__tests__/asc-jwt.test.ts (SETUP-02)
    - packages/core/__tests__/config-permissions.test.ts (SETUP-02)
    - cli/__tests__/doctor.test.ts (SETUP-03)
    - cli/__tests__/setup-firebase.test.ts (SETUP-04 — mocked subprocess tests)
    - packages/baas/__tests__/firebase-provision.test.ts (SETUP-04 stub)
    - packages/core/src/credential-registry.ts (confirm CredentialRegistry export)
    - packages/core/src/asc-jwt.ts (confirm ASC JWT sign + short-exp)
    - cli/src/preflight.ts (confirm runPreflight wires CredentialRegistry before pipeline)
    - cli/src/doctor.ts (confirm --deep tier + PEM validation + firebase/asc checks)
    - cli/src/setup/firebase.ts (confirm spawnSync argv-array, --no-localhost, error propagation)
    - cli/src/setup/index.ts (confirm SECTION_ORDER, --full flag, setup [section] command surface)
    - cli/src/entry.ts (confirm await runPreflight fix — commit 976cfe7 from UAT)
  </read_first>
  <action>
    Per D-03: read source files directly to extract concrete evidence.

    1. Run `pnpm test` at execution start (per D-01). Record exact output: "Test Files N passed / Tests M passed / K skipped / duration". This goes in the behavioral spot-checks table.

    2. For each SETUP-* requirement, extract:
       - Exact test file names (confirm existence via Read tool)
       - Assertion count per test file (count describe/it/test/expect calls or read runner output)
       - Commit hashes from SUMMARY.md files (read each SUMMARY file)
       - Key structural checks:
         * SETUP-01: confirm `cli/src/setup/index.ts` exports SECTION_ORDER with 8+ entries + `dtc setup [section]` + `--full` flag in cli.ts
         * SETUP-02: confirm `runPreflight` in `cli/src/preflight.ts` calls `runCredentialChecks` BEFORE the first LLM call (read and note the ordering); confirm `packages/core/src/credential-registry.ts` has a CredentialRegistry class
         * SETUP-03: confirm `cli/src/doctor.ts` has `--deep` tier and checks `firebase-tools`, service-account JSON, and ASC key file; confirm `runCredentialChecks` is shared between doctor and preflight
         * SETUP-04: confirm `cli/src/setup/firebase.ts` uses `spawnSync` with argv array (no `shell: true`); confirm no hardcoded 'MyApp' literal (D-02 guard from 03-VALIDATION.md)

    3. Run targeted Phase 3 vitest command:
       ```
       pnpm vitest run cli/__tests__/setup-wizard.test.ts cli/__tests__/preflight-credential-gate.test.ts packages/core/__tests__/credential-registry.test.ts packages/core/__tests__/asc-jwt.test.ts packages/core/__tests__/config-permissions.test.ts cli/__tests__/doctor.test.ts cli/__tests__/setup-firebase.test.ts packages/baas/__tests__/firebase-provision.test.ts
       ```
       Note: 03-VALIDATION.md reports 55 tests total; cross-check with live runner output.

    4. Confirm UAT status from 03-UAT.md:
       - Status should be complete (7 passed / 1 skipped)
       - Skipped test: Firebase live project creation (03-04-T2 — the human_needed item)
       - Note entry.ts await fix (commit 976cfe7) as a UAT-stage inline fix

    Assemble all evidence strings for Task 2.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -5</automated>
  </verify>
  <done>Live pnpm test output captured; Phase 3 test files confirmed green; commit hashes extracted from SUMMARY.md files; key structural checks confirmed (CredentialRegistry in preflight, doctor --deep tier, firebase spawnSync no shell:true, SECTION_ORDER present); UAT status confirmed 7/1 with Firebase live skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Write 03-VERIFICATION.md</name>
  <files>.planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-open-source-release-readiness/VERIFICATION.md (canonical format — replicate structure including human_verification section)
    - .planning/phases/08-formal-verification-foundation-setup/08-PATTERNS.md (human_needed frontmatter pattern, SETUP-04 table row pattern, human verification section pattern)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md`.

    Follow the Phase 1 VERIFICATION.md structure exactly, using the `human_needed` status variant. Required sections in order:

    1. YAML frontmatter:
       - `phase: 03-setup-and-diagnostics`
       - `verified: <current ISO-8601 timestamp>`
       - `status: human_needed`
       - `score: 4/4 requirements verified in code + 0/1 human checkpoint pending`
       - `overrides_applied: 0`
       - `human_verification:` array with one entry:
         - `test: "Run \`dtc setup firebase\` 'create new project' branch with a real Google account + real GCP billing account"`
         - `expected: "New Firebase project created, iOS app registered, GoogleService-Info.plist downloaded to project tree, config saved to ~/.dtc/config.json with firebaseProjectId. Subprocess errors surface verbatim on conflict."`
         - `why_human: "Requires live firebase CLI login and real GCP billing account. Task 03-04-T2 was explicitly designated as a human-verify checkpoint — cannot be automated in CI."`

    2. Document header:
       - `# Phase 3: Setup & Diagnostics Verification Report`
       - Phase Goal: "A first-time user can configure all required credentials in one guided flow and the pipeline fails fast with actionable status before any LLM spend"
       - Verified timestamp
       - Status: `human_needed (all code-level requirements PASS; Firebase live-project creation deferred to user)`

    3. `## Goal Achievement` section containing:
       a. `### Requirements Coverage` table — 4 rows, columns `# | REQ-ID | Description | Status | Evidence`:
          - SETUP-01: Status `PASS`, Evidence cites setup-wizard.test.ts, setup-sections.test.ts, SECTION_ORDER list, commits from 03-03-SUMMARY
          - SETUP-02: Status `PASS`, Evidence cites preflight-credential-gate.test.ts (5 integration tests), credential-registry.test.ts, asc-jwt.test.ts, config-permissions.test.ts, `runCredentialChecks` called before LLM spend in preflight.ts, commits from 03-01-SUMMARY + 03-02-SUMMARY
          - SETUP-03: Status `PASS`, Evidence cites doctor.test.ts, `--deep` tier, shared `runCredentialChecks`, firebase-tools + service-account + ASC checks, commits from 03-05-SUMMARY
          - SETUP-04: Status `PASS (code-level)`, Evidence cites setup-firebase.test.ts (mocked subprocess tests), firebase-provision.test.ts (stub), spawnSync argv-array (no shell: true), no 'MyApp' literal, commits from 03-04-SUMMARY. Append: "Live Google OAuth flow deferred — see human_verification."
       b. Score summary line
       c. `### Required Artifacts` table — list key files: credential-registry.ts, asc-jwt.ts, config.ts (chmod 0600), preflight.ts, cli/src/setup/ section files (index.ts + 8 sections), doctor.ts, prerequisites.ts, setup-firebase.ts + their test files
       d. `### Behavioral Spot-Checks` table — include: `pnpm test` (live output from Task 1), targeted Phase 3 vitest run, `grep -l 'runCredentialChecks' cli/src/preflight.ts cli/src/doctor.ts` (shared function), `grep 'shell:' cli/src/setup/firebase.ts` (should return nothing — no shell:true), `grep 'SECTION_ORDER' cli/src/setup/index.ts` (present)
       e. `### Anti-Patterns Found` table — note the entry.ts missing await pre-UAT (fixed in commit 976cfe7); include any pre-existing it.skip annotations
    4. `### Human Verification Required` section (mirroring Phase 1 format):
       - Header: "All items relate to live Firebase CLI login and real GCP billing — cannot be automated in CI."
       - Item 1: Real Firebase project creation via `dtc setup firebase`
         - Test: Run `dtc setup firebase` "create new project" branch with a real Google account (logged in via `firebase login --no-localhost`). Provide a unique project ID.
         - Expected: New Firebase project appears in Firebase Console; iOS app registered; `GoogleService-Info.plist` downloaded into the project tree; `~/.dtc/config.json` updated with `project.firebaseProjectId`. On conflict, subprocess error surfaces verbatim.
         - Why human: Requires live Firebase CLI login and real GCP billing account. 03-04-T2 was explicitly designated as a human-verify checkpoint.
    5. `### Gaps Summary` — "Zero code-level gaps remain. All 4 requirements are satisfied in the repository. One human checkpoint (SETUP-04 live Firebase project creation) remains outstanding."
    6. `### Verdict`:
       - `**GOAL_ACHIEVED in code**` — all 4 requirements satisfied in the repository as committed.
       - `**GAPS_REMAIN for live Firebase confirmation**` — the Firebase project creation flow requires a real GCP account; the tooling ships in `cli/src/setup/firebase.ts`; only the human action is outstanding.
       - `Status is \`human_needed\` rather than \`passed\` because SETUP-04's live Firebase step cannot be completed without a real GCP billing account.`
    7. Footer: `_Verified: <timestamp>_` / `_Verifier: Claude (gsd-verifier)_`

    UAT note to include under Behavioral Spot-Checks or in the verdict prose: "UAT (03-UAT.md): 7/8 tests passed, 1 skipped (test 5: Firebase live). Inline fix applied during UAT: entry.ts missing `await` before `runPreflight` call — fixed in commit 976cfe7."
  </action>
  <verify>
    <automated>grep -c "SETUP-0" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` exists
    - `grep "status: human_needed" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match in frontmatter
    - `grep -c "SETUP-0" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns at least 4
    - `grep "PASS (code-level)" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match (SETUP-04 row)
    - `grep "human_verification" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match (in frontmatter)
    - `grep "Human Verification Required" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match (section heading)
    - `grep "score: 4/4 requirements verified in code + 0/1 human checkpoint pending" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match
    - `grep "preflight-credential-gate.test.ts" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match (SETUP-02 evidence)
    - `grep "setup-firebase.test.ts" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match (SETUP-04 evidence)
    - `grep "GOAL_ACHIEVED" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match
    - `grep "976cfe7" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` returns a match (UAT inline fix commit)
  </acceptance_criteria>
  <done>03-VERIFICATION.md written with human_needed status; SETUP-04 row uses PASS (code-level) with human_verification section; all 4 SETUP-* rows have evidence with test file names + assertion counts + commit hashes; behavioral spot-checks include live pnpm test output; UAT inline fix commit 976cfe7 noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VALIDATION.md → VERIFICATION.md | Evidence read from existing planning artifacts and cited in new verification doc |
| human_needed classification | SETUP-04 must be correctly classified as code-level PASS with human checkpoint — risk of accidentally marking it fully PASS or fully FAIL |
| UAT inline fix | entry.ts await fix (commit 976cfe7) must be disclosed — risk of omitting a code change that happened during UAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | SETUP-04 classification in requirements table | mitigate | Action explicitly specifies `PASS (code-level)` status + inline note; acceptance_criteria greps for `PASS (code-level)` |
| T-08-02-02 | Information Disclosure | False negative on human_needed — SETUP-04 marked fully PASS without disclosing deferred live step | mitigate | D-02 enforced: frontmatter `status: human_needed`, `human_verification` block required; acceptance_criteria verifies `human_needed` in frontmatter |
| T-08-02-03 | Repudiation | UAT inline fix (entry.ts await, commit 976cfe7) omitted from VERIFICATION.md | mitigate | `read_first` includes `03-UAT.md` and `cli/src/entry.ts`; action text explicitly requires noting commit 976cfe7; acceptance_criteria grep for `976cfe7` |
| T-08-02-04 | Tampering | Incorrect assertion counts — Phase 3 has 55 tests total; executor might cite wrong subset | mitigate | Task 1 runs targeted vitest command live to get current assertion count; action specifies cross-checking with 03-VALIDATION.md audit |
| T-08-02-05 | Spoofing | Wrong phase goal text or misattributed success criteria | mitigate | `read_first` includes canonical template (Phase 1 VERIFICATION.md) and ROADMAP.md which has the verbatim Phase 3 success criteria |
</threat_model>

<verification>
After Task 2 completes:
- `cat .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` — review format matches Phase 1 template with human_needed variant
- `grep "status: human_needed" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md`
- `grep "PASS (code-level)" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md`
- `grep "GOAL_ACHIEVED" .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md`
</verification>

<success_criteria>
1. `.planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md` exists with `status: human_needed` frontmatter
2. `human_verification` YAML array present with Firebase live-project-creation test, expected outcome, and why_human
3. Requirements table has 4 rows (SETUP-01..04); SETUP-04 uses `PASS (code-level)` with inline note about deferred live flow
4. Behavioral spot-checks table includes live `pnpm test` output
5. UAT inline fix (entry.ts await, commit 976cfe7) disclosed
6. Verdict is `GOAL_ACHIEVED in code` / `GAPS_REMAIN for live Firebase`; status is `human_needed`
</success_criteria>

<output>
After completion, create `.planning/phases/08-formal-verification-foundation-setup/08-02-SUMMARY.md`
</output>
