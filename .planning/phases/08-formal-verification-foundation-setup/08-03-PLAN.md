---
phase: 08-formal-verification-foundation-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
  - 08-02-PLAN.md
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04

must_haves:
  truths:
    - "REQUIREMENTS.md traceability table has FOUND-01..04 marked 'Verified'"
    - "REQUIREMENTS.md traceability table has SETUP-01..04 marked 'Verified (code-level; SETUP-04 human step pending)'"
    - "The top of REQUIREMENTS.md still has checkboxes for each requirement correctly reflecting completion state"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table with Phase 8 verification status"
      contains: "Verified"
  key_links:
    - from: "REQUIREMENTS.md traceability rows FOUND-01..04"
      to: "02-VERIFICATION.md verdict"
      via: "Phase 8 gap closure"
      pattern: "FOUND-0[1-4].*Verified"
    - from: "REQUIREMENTS.md traceability rows SETUP-01..04"
      to: "03-VERIFICATION.md verdict"
      via: "Phase 8 gap closure"
      pattern: "SETUP-0[1-4].*Verified"
---

<objective>
Update the REQUIREMENTS.md traceability table to reflect Phase 8 verification results.

Purpose: The VERIFICATION.md documents created in Plans 01 and 02 formally establish that FOUND-01..04 and SETUP-01..04 are verified. This plan updates the canonical traceability table so the requirements status is queryable in one place.

Output: `.planning/REQUIREMENTS.md` — rows for FOUND-01..04 changed from `Pending` to `Verified`; rows for SETUP-01..04 changed from `Pending` to `Verified (code-level; SETUP-04 human step pending)`.
</objective>

<execution_context>
@/Users/rayliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rayliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-formal-verification-foundation-setup/08-PATTERNS.md

<interfaces>
<!-- Current REQUIREMENTS.md traceability table rows to update (lines 115-122) -->

Current state:
```
| FOUND-01 | Phase 2 → Phase 8 (gap closure) | Pending |
| FOUND-02 | Phase 2 → Phase 8 (gap closure) | Pending |
| FOUND-03 | Phase 2 → Phase 8 (gap closure) | Pending |
| FOUND-04 | Phase 2 → Phase 8 (gap closure) | Pending |
| SETUP-01 | Phase 3 → Phase 8 (gap closure) | Pending |
| SETUP-02 | Phase 3 → Phase 8 (gap closure) | Pending |
| SETUP-03 | Phase 3 → Phase 8 (gap closure) | Pending |
| SETUP-04 | Phase 3 → Phase 8 (gap closure) | Pending |
```

Target state:
```
| FOUND-01 | Phase 2 → Phase 8 (gap closure) | Verified |
| FOUND-02 | Phase 2 → Phase 8 (gap closure) | Verified |
| FOUND-03 | Phase 2 → Phase 8 (gap closure) | Verified |
| FOUND-04 | Phase 2 → Phase 8 (gap closure) | Verified |
| SETUP-01 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
| SETUP-02 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
| SETUP-03 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
| SETUP-04 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
```

Also update the top-section checkboxes (lines 23-26 and 30-33) from `- [ ]` to `- [x]` for FOUND-01..04 and SETUP-01..04:
- FOUND-01, FOUND-02, FOUND-03, FOUND-04 → `[x]`
- SETUP-01, SETUP-02, SETUP-03, SETUP-04 → `[x]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md traceability table and requirement checkboxes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the ENTIRE file before editing — must not corrupt other rows)
    - .planning/phases/02-foundation-hardening/02-VERIFICATION.md (confirm verdict is GOAL_ACHIEVED / passed before updating)
    - .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md (confirm verdict is GOAL_ACHIEVED in code / human_needed before updating)
  </read_first>
  <action>
    Read the full REQUIREMENTS.md file. Make exactly two categories of edits:

    CATEGORY 1 — Top section requirement checkboxes (Foundation Hardening and Setup & Diagnostics sections):
    Locate the lines in the `### Foundation Hardening` section that read:
    ```
    - [ ] **FOUND-01**: ...
    - [ ] **FOUND-02**: ...
    - [ ] **FOUND-03**: ...
    - [ ] **FOUND-04**: ...
    ```
    Change `- [ ]` to `- [x]` for each of FOUND-01 through FOUND-04.

    Locate the lines in the `### Setup & Diagnostics` section that read:
    ```
    - [ ] **SETUP-01**: ...
    - [ ] **SETUP-02**: ...
    - [ ] **SETUP-03**: ...
    - [ ] **SETUP-04**: ...
    ```
    Change `- [ ]` to `- [x]` for each of SETUP-01 through SETUP-04.

    CATEGORY 2 — Traceability table (the `## Traceability` section at the bottom):
    Find these 8 rows (exact current text):
    ```
    | FOUND-01 | Phase 2 → Phase 8 (gap closure) | Pending |
    | FOUND-02 | Phase 2 → Phase 8 (gap closure) | Pending |
    | FOUND-03 | Phase 2 → Phase 8 (gap closure) | Pending |
    | FOUND-04 | Phase 2 → Phase 8 (gap closure) | Pending |
    | SETUP-01 | Phase 3 → Phase 8 (gap closure) | Pending |
    | SETUP-02 | Phase 3 → Phase 8 (gap closure) | Pending |
    | SETUP-03 | Phase 3 → Phase 8 (gap closure) | Pending |
    | SETUP-04 | Phase 3 → Phase 8 (gap closure) | Pending |
    ```
    Replace with:
    ```
    | FOUND-01 | Phase 2 → Phase 8 (gap closure) | Verified |
    | FOUND-02 | Phase 2 → Phase 8 (gap closure) | Verified |
    | FOUND-03 | Phase 2 → Phase 8 (gap closure) | Verified |
    | FOUND-04 | Phase 2 → Phase 8 (gap closure) | Verified |
    | SETUP-01 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
    | SETUP-02 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
    | SETUP-03 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
    | SETUP-04 | Phase 3 → Phase 8 (gap closure) | Verified (code-level; SETUP-04 human step pending) |
    ```

    Do NOT change any other rows (REPO-*, NPM-*, FLOW-*, GATE-*, FIRE-*, TF-*, VAL-*, DESIGN-*, MCP-*, OBS-*).
    Do NOT change the file's last-updated footer line — it reflects the original roadmap revision date.

    Use the Edit tool (preferred) to make surgical changes rather than rewriting the entire file.
  </action>
  <verify>
    <automated>grep -c "Verified" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "FOUND-01.*Verified" .planning/REQUIREMENTS.md` returns a match (not "Pending")
    - `grep "FOUND-02.*Verified" .planning/REQUIREMENTS.md` returns a match
    - `grep "FOUND-03.*Verified" .planning/REQUIREMENTS.md` returns a match
    - `grep "FOUND-04.*Verified" .planning/REQUIREMENTS.md` returns a match
    - `grep "SETUP-01.*Verified (code-level" .planning/REQUIREMENTS.md` returns a match
    - `grep "SETUP-02.*Verified (code-level" .planning/REQUIREMENTS.md` returns a match
    - `grep "SETUP-03.*Verified (code-level" .planning/REQUIREMENTS.md` returns a match
    - `grep "SETUP-04.*Verified (code-level" .planning/REQUIREMENTS.md` returns a match
    - `grep "FOUND-01.*Pending" .planning/REQUIREMENTS.md` returns NO match (old status gone)
    - `grep "SETUP-01.*Pending" .planning/REQUIREMENTS.md` returns NO match
    - `grep "\- \[x\] \*\*FOUND-01" .planning/REQUIREMENTS.md` returns a match (checkbox updated)
    - `grep "\- \[x\] \*\*SETUP-01" .planning/REQUIREMENTS.md` returns a match (checkbox updated)
    - `grep "FIRE-01.*Pending" .planning/REQUIREMENTS.md` returns a match (unrelated rows unchanged)
    - `grep -c "Pending" .planning/REQUIREMENTS.md` returns the same count as before minus 8 (verify no accidental corruption)
  </acceptance_criteria>
  <done>REQUIREMENTS.md updated: FOUND-01..04 checkboxes set to [x] and traceability rows changed from Pending to Verified; SETUP-01..04 checkboxes set to [x] and traceability rows changed from Pending to Verified (code-level; SETUP-04 human step pending); all other rows unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VERIFICATION.md verdicts → REQUIREMENTS.md | Status update must accurately reflect the verification verdict — must not mark Verified if VERIFICATION.md is missing or shows failures |
| REQUIREMENTS.md edit → other rows | Surgical edit must not corrupt adjacent traceability rows (FIRE-*, TF-*, VAL-*, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | Corruption of non-FOUND/SETUP rows in traceability table | mitigate | `read_first` requires reading full REQUIREMENTS.md before editing; action explicitly states "do NOT change any other rows"; acceptance_criteria verifies FIRE-01 still has Pending (canary check) |
| T-08-03-02 | Information Disclosure | Marking Verified before reading VERIFICATION.md verdicts | mitigate | `read_first` includes both 02-VERIFICATION.md and 03-VERIFICATION.md; action specifies "confirm verdict is GOAL_ACHIEVED before updating" |
| T-08-03-03 | Tampering | Wrong status string — e.g., SETUP-01 marked `Verified` (without parenthetical) instead of `Verified (code-level; SETUP-04 human step pending)` | mitigate | Action provides exact replacement strings verbatim; acceptance_criteria grep for `Verified (code-level` to confirm parenthetical is present |
| T-08-03-04 | Repudiation | Checkbox update omitted (traceability table updated but top-section `- [ ]` remain unchecked) | mitigate | Action specifies two distinct edit categories; acceptance_criteria greps for both `[x] **FOUND-01` and `[x] **SETUP-01` |
</threat_model>

<verification>
After Task 1 completes:
- `grep "FOUND-01.*Pending" .planning/REQUIREMENTS.md` — must return nothing
- `grep "SETUP-04.*Verified (code-level" .planning/REQUIREMENTS.md` — must return 1 match
- `grep "FIRE-01.*Pending" .planning/REQUIREMENTS.md` — must still return a match (canary: other rows unchanged)
- `grep -c "Verified" .planning/REQUIREMENTS.md` — should return ≥ 8 (FOUND-01..04 = 4, SETUP-01..04 = 4, plus any pre-existing Verified rows)
</verification>

<success_criteria>
1. FOUND-01..04 traceability rows changed from `Pending` to `Verified`
2. SETUP-01..04 traceability rows changed from `Pending` to `Verified (code-level; SETUP-04 human step pending)`
3. FOUND-01..04 checkboxes changed from `[ ]` to `[x]` in the Foundation Hardening section
4. SETUP-01..04 checkboxes changed from `[ ]` to `[x]` in the Setup & Diagnostics section
5. All other traceability rows (FIRE-*, TF-*, VAL-*, DESIGN-*, MCP-*, OBS-*, REPO-*, NPM-*, FLOW-*, GATE-*) are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-formal-verification-foundation-setup/08-03-SUMMARY.md`
</output>
