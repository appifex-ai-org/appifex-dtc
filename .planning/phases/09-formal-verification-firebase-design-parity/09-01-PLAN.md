---
phase: 09-formal-verification-firebase-design-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-firebase-integration/04-VERIFICATION.md
autonomous: true
requirements:
  - FIRE-01
  - FIRE-02
  - FIRE-03
  - FIRE-04
  - FIRE-05

must_haves:
  truths:
    - "04-VERIFICATION.md exists and its frontmatter status is 'human_needed'"
    - "Every FIRE-01..05 row in the requirements table cites a test file name, assertion count, and commit hash"
    - "The behavioral spot-checks table includes a live pnpm test output row (files / passed / skipped)"
    - "FIRE-04 row uses 'PASS (code-level)' with a human_needed note citing the live Firebase run"
    - "The human_verification block describes running dtc against a real Firebase project"
    - "Score frontmatter is '5/5 requirements verified in code + 0/1 human checkpoint pending'"
    - "Verdict says GOAL_ACHIEVED in code"
  artifacts:
    - path: ".planning/phases/04-firebase-integration/04-VERIFICATION.md"
      provides: "Formal verification report for Phase 4 Firebase Integration"
      contains: "FIRE-01"
  key_links:
    - from: "04-VERIFICATION.md requirements table"
      to: "04-VALIDATION.md evidence"
      via: "test file names and assertion counts"
      pattern: "FIRE-0[1-5]"
    - from: "04-VERIFICATION.md FIRE-04 row"
      to: "human_verification section"
      via: "human_needed pattern from 03-VERIFICATION.md"
      pattern: "human_needed"
    - from: "04-VERIFICATION.md behavioral spot-checks"
      to: "live pnpm test output"
      via: "D-03 requirement — run pnpm test at execution start"
      pattern: "pnpm test"
---

<objective>
Write the Phase 4 Firebase Integration formal verification report.

Purpose: Formally close FIRE-01..05 by producing a 04-VERIFICATION.md that proves all Phase 4 success criteria are satisfied in the repository. FIRE-04 (live Firebase provisioning) is PASS (code-level) with a human_needed checkpoint for the live Firebase run.

Output: `.planning/phases/04-firebase-integration/04-VERIFICATION.md` — a complete verification report following the Phase 1 VERIFICATION.md format exactly, with `status: human_needed` and `score: 5/5 requirements verified in code + 0/1 human checkpoint pending`.
</objective>

<execution_context>
@/Users/rayliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rayliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-open-source-release-readiness/VERIFICATION.md
@.planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md
@.planning/phases/04-firebase-integration/04-VALIDATION.md
@.planning/phases/04-firebase-integration/04-UAT.md
@.planning/phases/09-formal-verification-firebase-design-parity/09-CONTEXT.md
@.planning/phases/09-formal-verification-firebase-design-parity/09-RESEARCH.md

<interfaces>
<!-- Format contract extracted from Phase 1 VERIFICATION.md (the canonical template) -->
<!-- human_needed pattern extracted from 03-VERIFICATION.md -->

YAML frontmatter shape (Phase 4 — has human_verification block for FIRE-04):
```yaml
---
phase: 04-firebase-integration
verified: <ISO-8601 timestamp>
status: human_needed
score: 5/5 requirements verified in code + 0/1 human checkpoint pending
overrides_applied: 0
human_verification:
  - test: "Run `dtc` against a real Firebase project with valid firebase-admin credentials"
    expected: "GoogleService-Info.plist appears in the project tree; Firestore security rules are deployed to the live project; subsequent runs are idempotent (checkpoint prevents re-creation)"
    why_human: "FIRE-04 live Firebase project creation/plist download/rules deployment cannot be automated in CI — requires a real Firebase project and Apple Developer account. Code-level evidence (firebase-provision.test.ts, 7 assertions) proves the implementation is correct."
---
```

Requirements table columns: `# | REQ-ID | Description | Status | Evidence`
Each Evidence cell must cite: test file name(s) + assertion count + commit hash + key structural check.
FIRE-04 evidence must end with: "Live Firebase provisioning deferred — see human_verification."

Required Artifacts table columns: `Artifact | Status | Details`
Behavioral Spot-Checks table columns: `Behavior | Command | Result | Status`
Anti-Patterns Found table columns: `File | Pattern | Severity | Impact`
Verdict section must use: `**GOAL_ACHIEVED in code**` (matching 03-VERIFICATION.md pattern).

Phase 4 evidence map from 09-RESEARCH.md:
- FIRE-01 (AppDelegate + SPM): test file `packages/baas/__tests__/firebase-codegen.test.ts` — 3 assertions — GREEN; source `packages/baas/src/templates/firebase/app-entry.swift.eta`; commits: 04-02 (`d26bdc9`, `0f1cdca`)
- FIRE-02 (hashed-nonce + REVERSED_CLIENT_ID): test file `packages/baas/__tests__/firebase-auth.test.ts` — 7 assertions — GREEN; sources `auth-manager.swift.eta`, `login-view.swift.eta`, `patchProjectDependencies`; commits: 04-02 (`d26bdc9`, `0f1cdca`)
- FIRE-03 (Codable + realtime + offline): test file `packages/baas/__tests__/firebase-data.test.ts` — 5 assertions — GREEN; sources `data-service.swift.eta`, `app-entry.swift.eta`; commits: 04-02 (`d26bdc9`, `0f1cdca`)
- FIRE-04 (firebase_provision idempotent + checkpointed): test file `packages/baas/__tests__/firebase-provision.test.ts` — 7 assertions — GREEN (includes overwritePlist from 04-07); source `packages/baas/src/firebase-provision.ts`; commits: 04-04 (`d9d9a2a`, `979b29a`, `3c82bc3`, `ea28e16`), 04-05 (`f07ed4c`), 04-07 (`83cfc3f`, `2475e04`, `6f03460`); post-plan: `8ba33c8` (FIRE-04 plist path mismatch fix)
- FIRE-05 (security lint hard-fail): test files `packages/baas/__tests__/firebase-security-lint.test.ts` (4 assertions) + `packages/baas/__tests__/render-templates.test.ts` (14 total, includes deny-all assertions) — all GREEN; sources `security.rules.eta`, `security-lint.ts`; commits: 04-03 (BANNED_PATTERNS), 04-06 (`c81f548`); post-plan: `6ea7779` (cross-user regex order-independent), `07a7b8d` (rules read path fix)

Phase 4 aggregate targeted tests: 26 passing (5 test files)
Full suite at execution time: 175 test files, 1612 tests passed, 8 skipped
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather evidence — run tests and read Phase 4 source files directly</name>
  <files>
    (read-only task — no files written; evidence assembled in memory for Task 2)
  </files>
  <read_first>
    - .planning/phases/04-firebase-integration/04-VALIDATION.md (per-task verification map + audit counts)
    - .planning/phases/04-firebase-integration/04-UAT.md (UAT results and status)
    - .planning/phases/04-firebase-integration/04-01-SUMMARY.md (FIRE-01 types commits)
    - .planning/phases/04-firebase-integration/04-02-SUMMARY.md (FIRE-01/02/03 template commits)
    - .planning/phases/04-firebase-integration/04-03-SUMMARY.md (FIRE-05 BANNED_PATTERNS commits)
    - .planning/phases/04-firebase-integration/04-04-SUMMARY.md (FIRE-04 firebase-provision module commits)
    - .planning/phases/04-firebase-integration/04-05-SUMMARY.md (FIRE-04 pipeline wiring commits)
    - .planning/phases/04-firebase-integration/04-06-SUMMARY.md (FIRE-05 security.rules.eta commits)
    - .planning/phases/04-firebase-integration/04-07-SUMMARY.md (FIRE-04 overwritePlist commits)
    - packages/baas/__tests__/firebase-codegen.test.ts (FIRE-01 — read actual test assertions)
    - packages/baas/__tests__/firebase-auth.test.ts (FIRE-02 — read actual test assertions)
    - packages/baas/__tests__/firebase-data.test.ts (FIRE-03 — read actual test assertions)
    - packages/baas/__tests__/firebase-provision.test.ts (FIRE-04 — read actual test assertions, confirm 7 tests)
    - packages/baas/__tests__/firebase-security-lint.test.ts (FIRE-05 — read actual test assertions, confirm 4 tests)
    - packages/baas/src/templates/firebase/app-entry.swift.eta (FIRE-01: confirm @UIApplicationDelegateAdaptor + FirebaseApp.configure + PersistentCacheSettings)
    - packages/baas/src/templates/firebase/auth-manager.swift.eta (FIRE-02: confirm SHA-256 hashed nonce + signInWithApple + signInWithGoogle)
    - packages/baas/src/templates/firebase/security.rules.eta (FIRE-05: confirm deny-all catch-all block)
    - packages/baas/src/firebase-provision.ts (FIRE-04: confirm runFirebaseProvision, overwritePlist option, idempotency guard)
    - packages/baas/src/security-lint.ts (FIRE-05: confirm lintSecurityRules, BANNED_PATTERNS, DENY_ALL_PATTERN, SecurityLintError)
  </read_first>
  <action>
    Per D-03: do not summarize from SUMMARY.md alone — read source files directly to extract:

    1. Run `pnpm test` at execution start (per D-03). Record exact output line: "Test Files N passed / Tests M passed / K skipped / duration". This goes in the behavioral spot-checks table.

    2. Run targeted Phase 4 vitest command to get isolated confirmation:
       ```
       pnpm vitest run packages/baas/__tests__/firebase-codegen.test.ts packages/baas/__tests__/firebase-auth.test.ts packages/baas/__tests__/firebase-data.test.ts packages/baas/__tests__/firebase-provision.test.ts packages/baas/__tests__/firebase-security-lint.test.ts
       ```
       Expected: 5 files passed / 26 tests passed. Record the exact output.

    3. For each FIRE-* requirement, extract from source:
       - FIRE-01: Count `it(` / `test(` / `describe(` blocks in firebase-codegen.test.ts (expect 3). Confirm `@UIApplicationDelegateAdaptor` appears in app-entry.swift.eta. Confirm no CocoaPods reference.
       - FIRE-02: Count assertions in firebase-auth.test.ts (expect 7). Confirm `SHA256` and `hashed-nonce` pattern in auth-manager.swift.eta. Confirm `REVERSED_CLIENT_ID` in patchProjectDependencies or project.yml generation code.
       - FIRE-03: Count assertions in firebase-data.test.ts (expect 5). Confirm `addSnapshotListener` + `PersistentCacheSettings` in app-entry.swift.eta or data-service.swift.eta.
       - FIRE-04: Count assertions in firebase-provision.test.ts (expect 7 — includes 2 overwritePlist tests from 04-07). Confirm `idempotency` guard (checkpoint check) in firebase-provision.ts. Extract commit hashes from SUMMARY files.
       - FIRE-05: Count assertions in firebase-security-lint.test.ts (expect 4). Confirm `match /{document=**} allow read, write: if false` in security.rules.eta. Confirm `SecurityLintError` throws (no override path).

    4. Confirm UAT status from 04-UAT.md (should be `status: complete`, all 5 tests pass).

    5. Extract commit hashes from each SUMMARY.md (read SUMMARY files for commit SHAs: 04-02 → d26bdc9 etc.).

    Assemble all evidence strings — they will be written verbatim into the VERIFICATION.md in Task 2.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 with all 5 Phase 4 test files green
    - Live test output captured (file count, test count, skipped count, duration)
    - Targeted vitest run shows 5 files / 26 tests passed
    - `@UIApplicationDelegateAdaptor` confirmed in app-entry.swift.eta
    - `match /{document=**} allow read, write: if false` confirmed in security.rules.eta
    - 7 test assertions confirmed in firebase-provision.test.ts (live count)
    - Commit hashes extracted from 04-02-SUMMARY.md through 04-07-SUMMARY.md
  </acceptance_criteria>
  <done>Live pnpm test output captured; all 5 Phase 4 test files confirmed green with exact assertion counts; commit hashes extracted from SUMMARY.md files; structural checks confirmed (AppDelegate adaptor, hashed nonce, deny-all rule, idempotency guard, SecurityLintError).</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-VERIFICATION.md</name>
  <files>.planning/phases/04-firebase-integration/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/01-open-source-release-readiness/VERIFICATION.md (canonical format — replicate structure exactly)
    - .planning/phases/03-setup-and-diagnostics/03-VERIFICATION.md (human_needed pattern for FIRE-04 row and human_verification block)
    - .planning/phases/08-formal-verification-foundation-setup/08-VERIFICATION.md (Observable Truths table format)
  </read_first>
  <action>
    Use the Write tool to create `.planning/phases/04-firebase-integration/04-VERIFICATION.md`.

    Follow the Phase 1 VERIFICATION.md structure exactly with the human_needed pattern from 03-VERIFICATION.md. Required sections in order:

    1. YAML frontmatter:
       ```yaml
       ---
       phase: 04-firebase-integration
       verified: <current ISO-8601 timestamp>
       status: human_needed
       score: 5/5 requirements verified in code + 0/1 human checkpoint pending
       overrides_applied: 0
       human_verification:
         - test: "Run `dtc` against a real Firebase project with valid firebase-admin credentials and a configured Apple Developer account"
           expected: "GoogleService-Info.plist appears in the project tree; Firestore security rules are deployed; subsequent run is idempotent (checkpoint skips re-creation)"
           why_human: "FIRE-04 live Firebase project creation, plist download, and rules deployment cannot be automated in CI — requires real Firebase credentials and Apple Developer account. Code-level: firebase-provision.test.ts (7 assertions) proves idempotency, checkpoint integration, plist-download path, ProvisionError on non-zero exit, and overwritePlist behavior."
       ---
       ```

    2. Document header: `# Phase 4: Firebase Integration Verification Report`
       - Phase Goal (from ROADMAP.md)
       - Verified: <timestamp>
       - Status: human_needed
       - Re-verification: No — initial verification

    3. `## Goal Achievement` section containing:

       a. `### Observable Truths` table — truths that are grep-verifiable claims about artifacts. Include rows such as:
          - app-entry.swift.eta contains `@UIApplicationDelegateAdaptor`
          - app-entry.swift.eta contains `FirebaseApp.configure()` and `PersistentCacheSettings`
          - auth-manager.swift.eta contains SHA-256 hashed nonce
          - security.rules.eta contains deny-all catch-all block
          - security-lint.ts throws `SecurityLintError` (no override path)
          - firebase-provision.ts has idempotency guard (checkpoint check before provisioning)
          - firebase-codegen.test.ts passes (3 assertions GREEN)
          - firebase-auth.test.ts passes (7 assertions GREEN)
          - firebase-data.test.ts passes (5 assertions GREEN)
          - firebase-provision.test.ts passes (7 assertions GREEN — includes overwritePlist from 04-07)
          - firebase-security-lint.test.ts passes (4 assertions GREEN)
          - Live pnpm test suite: 175 files / 1612 tests / 8 skipped (captured from Task 1)

       b. `### Roadmap Success Criteria` table — 5 rows matching Phase 4 success criteria from ROADMAP.md:
          1. AppDelegate + SPM (FIRE-01)
          2. Email/Apple/Google auth + hashed-nonce + REVERSED_CLIENT_ID (FIRE-02)
          3. Codable Firestore + realtime listeners + offline caching (FIRE-03)
          4. firebase_provision idempotent + checkpointed (FIRE-04, code-level)
          5. Security lint hard-fail (FIRE-05)

       c. `### Requirements Coverage` table — 5 rows, columns: `# | REQ-ID | Description | Status | Evidence`. Concrete evidence cells:
          - FIRE-01 | `@UIApplicationDelegateAdaptor` in AppDelegate; Firebase SDK via SPM only | PASS | `packages/baas/__tests__/firebase-codegen.test.ts` (3 assertions GREEN). Source: `packages/baas/src/templates/firebase/app-entry.swift.eta` — `@UIApplicationDelegateAdaptor` confirmed. No CocoaPods reference. Commits: `d26bdc9`, `0f1cdca` (04-02).
          - FIRE-02 | hashed-nonce + REVERSED_CLIENT_ID | PASS | `packages/baas/__tests__/firebase-auth.test.ts` (7 assertions GREEN). Sources: `auth-manager.swift.eta` (SHA-256 hashed nonce confirmed), `login-view.swift.eta`, `patchProjectDependencies` (REVERSED_CLIENT_ID injection). Commits: `d26bdc9`, `0f1cdca` (04-02).
          - FIRE-03 | Codable + realtime + offline | PASS | `packages/baas/__tests__/firebase-data.test.ts` (5 assertions GREEN). Sources: `data-service.swift.eta` (`addSnapshotListener`, `ListenerRegistration`, `deinit` cleanup), `app-entry.swift.eta` (`PersistentCacheSettings`). Commits: `d26bdc9`, `0f1cdca` (04-02).
          - FIRE-04 | firebase_provision idempotent + checkpointed | PASS (code-level) | `packages/baas/__tests__/firebase-provision.test.ts` (7 assertions GREEN — includes 2 overwritePlist tests from Plan 04-07). Source: `firebase-provision.ts` (idempotency guard, checkpoint integration, `@clack/prompts` overwrite confirm). Commits: `d9d9a2a`, `979b29a`, `3c82bc3`, `ea28e16` (04-04); `f07ed4c` (04-05); `83cfc3f`, `2475e04`, `6f03460` (04-07); `8ba33c8` (FIRE-04 plist path mismatch fix — quick task 260417-tzh). **Live Firebase provisioning deferred — see human_verification.**
          - FIRE-05 | security lint hard-fail | PASS | `packages/baas/__tests__/firebase-security-lint.test.ts` (4 assertions GREEN) + `packages/baas/__tests__/render-templates.test.ts` (includes deny-all assertions). Sources: `security.rules.eta` (deny-all: `match /{document=**} allow read, write: if false`), `security-lint.ts` (`SecurityLintError` thrown, no override). Commits: 04-03 (BANNED_PATTERNS), `c81f548` (04-06), `6ea7779` (cross-user regex), `07a7b8d` (rules read path).

       d. `### Required Artifacts` table — list key files created by Phase 4:
          - `packages/baas/src/templates/firebase/app-entry.swift.eta` — VERIFIED — AppDelegate with @UIApplicationDelegateAdaptor + FirebaseApp.configure + PersistentCacheSettings
          - `packages/baas/src/templates/firebase/auth-manager.swift.eta` — VERIFIED — SHA-256 hashed nonce, signInWithApple, signInWithGoogle
          - `packages/baas/src/templates/firebase/login-view.swift.eta` — VERIFIED — SignInWithAppleButton (Apple HIG order)
          - `packages/baas/src/templates/firebase/data-service.swift.eta` — VERIFIED — addSnapshotListener, ListenerRegistration, deinit cleanup
          - `packages/baas/src/templates/firebase/security.rules.eta` — VERIFIED — deny-all catch-all block
          - `packages/baas/src/firebase-provision.ts` — VERIFIED — runFirebaseProvision, overwritePlist option, idempotency guard
          - `packages/baas/src/security-lint.ts` — VERIFIED — lintSecurityRules, BANNED_PATTERNS, DENY_ALL_PATTERN, SecurityLintError

       e. `### Behavioral Spot-Checks` table — include: live pnpm test output (Task 1 result), targeted Phase 4 vitest run (5 files / 26 tests), deny-all grep in security.rules.eta, @UIApplicationDelegateAdaptor grep in app-entry.swift.eta, SecurityLintError grep in security-lint.ts, idempotency guard grep in firebase-provision.ts

       f. `### Human Verification Required` section — present when status: human_needed. Format from 03-VERIFICATION.md:
          ```
          **Status:** human_needed — FIRE-04 live Firebase provisioning step pending
          **Test:** Run `dtc` against a real Firebase project with valid firebase-admin credentials and a configured Apple Developer account
          **Expected:** GoogleService-Info.plist appears in the project tree; Firestore security rules are deployed; subsequent run is idempotent
          **Scope:** Tight — analogous to SETUP-04 in Phase 3. Does not require testing all 5 FIRE-* requirements live.
          ```

       g. `### Gaps Summary` — "Zero code-level gaps remain. FIRE-01..03 and FIRE-05 are fully satisfied. FIRE-04 has one live Firebase run deferred to human verification."

       h. `### Verdict`:
          ```
          **GOAL_ACHIEVED in code** — 5/5 requirements are verified at the code level. The repository contains every template, provisioning module, and security lint guard required by the phase goal.
          **GAPS_REMAIN for live Firebase confirmation** — FIRE-04 human checkpoint (live Firebase provisioning run) is pending.
          Status is `human_needed`.
          ```

    4. Footer: `_Verified: <timestamp>_` / `_Verifier: Claude (gsd-verifier)_`

    Phase 4 aggregate: 5 targeted test files, 26 assertions, all green.
    Post-plan review fix commits to include: `4ecc459`, `6ea7779`, `7693a7b`, `07a7b8d`, `8ba33c8`.
  </action>
  <verify>
    <automated>grep -c "FIRE-0" .planning/phases/04-firebase-integration/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-firebase-integration/04-VERIFICATION.md` exists
    - `grep "status: human_needed" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match in frontmatter
    - `grep "score: 5/5 requirements verified in code" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match
    - `grep -c "FIRE-0" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns at least 5
    - `grep "GOAL_ACHIEVED" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match
    - `grep "human_needed" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns at least 2 matches (frontmatter + body)
    - `grep "firebase-codegen.test.ts" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match (FIRE-01 evidence)
    - `grep "firebase-provision.test.ts" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match (FIRE-04 evidence)
    - `grep "firebase-security-lint.test.ts" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match (FIRE-05 evidence)
    - `grep "render-templates.test.ts" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match (FIRE-05 second evidence file)
    - `grep "human_verification" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match (frontmatter block)
    - `grep "phase: 04-firebase-integration" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns a match in YAML frontmatter
    - `grep "pnpm test" .planning/phases/04-firebase-integration/04-VERIFICATION.md` returns at least 1 match (behavioral spot-checks)
  </acceptance_criteria>
  <done>04-VERIFICATION.md written, all 5 FIRE-* requirements have evidence rows with test file names + assertion counts + commit hashes, FIRE-04 row has human_needed note, human_verification block present in frontmatter, behavioral spot-checks table includes live pnpm test output, verdict is GOAL_ACHIEVED in code with status human_needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VALIDATION.md → VERIFICATION.md | Evidence read from existing planning artifacts and cited in new verification doc — risk of misquoting or cherry-picking |
| Source files → evidence claims | Executor reads source files directly; risk of citing incorrect line numbers, wrong commit hashes, or stale assertion counts |
| VERIFICATION.md → REQUIREMENTS.md | VERIFICATION.md verdict drives REQUIREMENTS.md status update in Plan 03 — a false PASS here propagates upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Evidence cells in requirements table | mitigate | Task 1 reads source files directly (not SUMMARY.md alone) per D-02; Task 2 action specifies exact test file names and commit sources to cite |
| T-09-01-02 | Information Disclosure | False negative — requirement marked PASS when test actually fails | mitigate | D-03 mandates running `pnpm test` live at execution start; spot-checks table must cite the live output |
| T-09-01-03 | Repudiation | Executor skips reading source files and writes from memory/summaries | mitigate | `<read_first>` lists all source files the executor MUST read before writing; acceptance_criteria require specific test file names appear in the output |
| T-09-01-04 | Elevation of Privilege | FIRE-04 marked fully Verified (not human_needed) — suppresses required human checkpoint | mitigate | Frontmatter `status: human_needed` enforced by acceptance_criteria grep; `human_verification` block required in both frontmatter and body |
| T-09-01-05 | Tampering | Missing render-templates.test.ts in FIRE-05 evidence (known pitfall from 09-RESEARCH.md) | mitigate | Acceptance criteria explicitly requires `grep "render-templates.test.ts"` returns a match |
</threat_model>

<verification>
After Task 2 completes:
- `cat .planning/phases/04-firebase-integration/04-VERIFICATION.md` — review format matches Phase 1 template
- `grep "status: human_needed" .planning/phases/04-firebase-integration/04-VERIFICATION.md` — frontmatter status correct
- `grep -c "FIRE-0" .planning/phases/04-firebase-integration/04-VERIFICATION.md` — returns ≥ 5
- `grep "GOAL_ACHIEVED" .planning/phases/04-firebase-integration/04-VERIFICATION.md` — verdict present
- `grep "human_verification" .planning/phases/04-firebase-integration/04-VERIFICATION.md` — human checkpoint present
</verification>

<success_criteria>
1. `.planning/phases/04-firebase-integration/04-VERIFICATION.md` exists with `status: human_needed` frontmatter
2. Requirements table has 5 rows (FIRE-01..05), each with: test file name(s), assertion count, commit hash, structural check result
3. FIRE-04 row uses "PASS (code-level)" with "Live Firebase provisioning deferred — see human_verification"
4. `human_verification` array present in YAML frontmatter with Firebase live-run test description
5. Behavioral spot-checks table includes live `pnpm test` output row with actual file/test counts
6. Verdict is `GOAL_ACHIEVED in code` with `status: human_needed` and `score: 5/5 requirements verified in code + 0/1 human checkpoint pending`
7. Both `firebase-security-lint.test.ts` AND `render-templates.test.ts` cited in FIRE-05 evidence
</success_criteria>

<output>
After completion, create `.planning/phases/09-formal-verification-firebase-design-parity/09-01-SUMMARY.md`
</output>
